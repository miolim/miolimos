<%# #695 (Hans): Chips für die Tags einer Kommunikation. Stream-replaced bei
    add/remove via CommunicationTagsController. Spiegelt tasks/_tags_chips. %>
<div id="comm_tags_chips_<%= comm.id %>" class="flex flex-wrap gap-1.5">
  <% Array(comm.tags).sort.each do |tag| %>
    <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs <%= tag_color_classes(tag) %>"
          <% if (d = tag_description(tag)).present? %>title="<%= d %>"<% end %>>
      <% if (ic = tag_icon_html(tag, size: "w-3 h-3")) %>
        <span class="inline-flex items-center opacity-70"><%= ic %></span>
      <% end %>
      <span><%= tag %></span>
      <%= button_to communication_tag_path(comm, tag: tag), method: :delete,
            form: { class: "inline m-0 p-0", data: { turbo_stream: true } },
            title: t("communications.remove_tag"),
            "aria-label": t("communications.remove_tag_named", tag: tag),
            class: "p-0 bg-transparent border-0 cursor-pointer leading-none text-slate-400 hover:text-rose-600" do %>
        <span class="text-sm leading-none">×</span>
      <% end %>
    </span>
  <% end %>
</div>
