<% content_for :title, t("awaitings.edit") %>

<div class="bg-white rounded border border-slate-200 p-4">
  <h1 class="text-lg font-semibold mb-3"><%= t("awaitings.edit") %></h1>
  <%= render "form", awaiting: @awaiting %>
</div>
